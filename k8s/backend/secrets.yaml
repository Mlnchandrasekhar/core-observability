apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: core-backend-secrets
  namespace: core-observe
spec:
  provider: gke
  parameters:
    secrets: |
      - resourceName: "projects/kfcpl-dev/secrets/core-observe/versions/latest"
        path: ".env"